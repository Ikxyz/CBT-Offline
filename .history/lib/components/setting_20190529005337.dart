import 'package:cbt_offline/import.dart';

class SettingCompon extends StatelessWidget {
  const SettingCompon({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}