import 'package:cbt_offline/import.dart';

class PerpareResultComponents extends StatelessWidget {
  const PerpareResultComponents({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
   return Container(
       child: Center(child: Text('Add Question route'),),
    );
  }
}