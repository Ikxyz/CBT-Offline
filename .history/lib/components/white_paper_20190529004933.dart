import 'package:cbt_offline/import.dart';

class WhitePaperComponents extends StatelessWidget {
  const WhitePaperComponents({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Whiet Board Papal'),),
    );
  }
}