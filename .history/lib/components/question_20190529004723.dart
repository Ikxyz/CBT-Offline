import 'package:cbt_offline/import.dart';

class QuestionComponnets extends StatefulWidget {
  QuestionComponnets({Key key}) : super(key: key);

  _QuestionComponentsState createState() => _QuestionComponnetsState();
}

class _QuestionComponnetsState extends State<QuestionComponnets> {
  @override
  Widget build(BuildContext context) {
    return Container(
       child: Center(child: Text('Add Question route'),),
    );
  }
}