import 'package:cbt_offline/import.dart';

class Perpare extends StatelessWidget {
  const Perpare({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}