import 'package:cbt_offline/import.dart';

class PerpareResult extends StatelessWidget {
  const PerpareResult({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}