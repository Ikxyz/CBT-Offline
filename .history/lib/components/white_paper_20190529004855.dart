import 'package:cbt_offline/import.dart';

class WhitePa extends StatelessWidget {
  const WhitePa({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}