import 'package:cbt_offline/import.dart';

class SettingCompo extends StatelessWidget {
  const SettingCompo({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}