import 'package:cbt_offline/import.dart';

class PerpareResultComponen extends StatelessWidget {
  const PerpareResultComponen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}