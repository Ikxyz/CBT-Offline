import 'package:cbt_offline/import.dart';

class WhitePaperCompo extends StatelessWidget {
  const WhitePaperCompo({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}