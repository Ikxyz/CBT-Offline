import 'package:cbt_offline/import.dart';

class QuestionComponnets extends StatefulWidget {
  _QuestionRouteState({Key key}) : super(key: key);

  __QuestionRouteStateState createState() => __QuestionRouteStateState();
}

class __QuestionRouteStateState extends State<_QuestionRouteState> {
  @override
  Widget build(BuildContext context) {
    return Container(
       child: Center(child: Text('Add Question route'),),
    );
  }
}