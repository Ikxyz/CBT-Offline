import 'import.dart';

class SplashScre extends StatelessWidget {
  const SplashScre({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}