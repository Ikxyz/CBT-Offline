import 'package:cbt_offline/import.dart';

class Qu extends StatefulWidget {
  Qu({Key key}) : super(key: key);

  _QuState createState() => _QuState();
}

class _QuState extends State<Qu> {
  @override
  Widget build(BuildContext context) {
    return Container(
       child: child,
    );
  }
}