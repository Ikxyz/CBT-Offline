import 'package:cbt_offline/import.dart';

class QuestionRoute extends StatefulWidget {
  QuestionRoute({Key key}) : super(key: key);

  _QuestionRouteState createState() => _QuestionRouteState();
}

class _QuestionRouteState extends State<QuestionRoute> {
  @override
  Widget build(BuildContext context) {
    return Container(
       child: Center(child: Text(''),),
    );
  }
}